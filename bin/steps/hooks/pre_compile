#!/usr/bin/env bash

echo "precompile-installing mysql"
apt-get update
apt-get install -y mysql-server libmysqlclient-dev
echo "done installing mysql"

if [ -f bin/pre_compile ]; then
  echo "-----> Running pre-compile hook"
  chmod +x bin/pre_compile
  ./bin/pre_compile
fi
